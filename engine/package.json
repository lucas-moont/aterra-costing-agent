{
  "name": "aterra-costing-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic costing engine — turns extraction.json into a costed quotation.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "cost": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
